<div id="main-columnR">
  <h1 class="page-header">Items</h1>
  <%= render "search" %>
  <div class="panel panel-default">
    <!-- Table -->
    <%= paginate @items %>
    <table class="table">
      <tr>
        <th><%= Item.human_attribute_name(:name) %></th>
        <th><%= Item.human_attribute_name(:description) %></th>
        <th><%= Item.human_attribute_name(:category_id) %></th>
        <th><%= Item.human_attribute_name(:price) %></th>
        <th><%= Item.human_attribute_name(:in_stock_now) %></th>
      </tr>
      <% @items.each do |item| %>
        <tr>
          <td> <%= link_to item.name, item_path(item) %> </td>
          <td> <%= item.description %> </td>
          <td> <%= item.category_id %> </td>
          <td> <%= item.price %> </td>
          <td> <%= item.in_stock_now %> </td>
          <td><%= button_to "Delete", item_path(item), :class => "btn btn-danger", :method => :delete %></td>
        </tr>
      <% end %>
    </table>
    <%= paginate @items %>
  </div>
  <%= button_to "Add Item", new_item_path, :class => "btn btn-success", :method => :get %>
</div>


